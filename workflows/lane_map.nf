workflow LANE_MAP {

    take:

    main:
        // Check if reads > 100

        // If reprocessing update read type

        // Create fastqs if needed (?)

        // Downsample for novaseq pool

        // run fastx

        // map
}